{% extends 'base.html.twig' %}

{% block title %}Adress{% endblock %}

{% block body %}
    <h1>Adress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adress.id }}</td>
            </tr>
            <tr>
                <th>AdressLine</th>
                <td>{{ adress.adressLine }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ adress.postalCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_adress_index') }}">back to list</a>

    <a href="{{ path('app_admin_adress_edit', {'id': adress.id}) }}">edit</a>

    {{ include('admin_adress/_delete_form.html.twig') }}
{% endblock %}
