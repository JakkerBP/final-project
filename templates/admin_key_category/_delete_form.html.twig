<form method="post" action="{{ path('app_admin_key_category_delete', {'id': key_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ key_category.id) }}">
    <button class="btn">Delete</button>
</form>
